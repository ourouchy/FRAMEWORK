{
  "name": "framework",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:build": "esbuild src/index.ts --bundle --outfile=dist/index.js --watch",
    "start:db": "json-server -w db.json --port 4000",
    "start": "concurrently npm:start:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.7.9",
    "concurrently": "^9.1.2",
    "esbuild": "^0.24.2"
  }
}
